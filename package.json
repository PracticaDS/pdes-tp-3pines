{
  "name": "RevolucionIndustrial",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://practicads.github.io/pdes-tp-3pines/",
  "scripts": {
    "commit-hook": "cd front-end/ && yarn lint && cd ../back-end/",
    "ci": "cd front-end/ && yarn install && yarn test && yarn lint && cd ../back-end/ && yarn install && yarn test-ci"
  },
  "devDependencies": {
    "husky": "^2.2.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "CI=true yarn commit-hook"
    }
  },
  "dependencies": {
    "chai": "^4.2.0",
    "http-status-codes": "^1.3.2",
    "mongoose": "^5.5.14"
  }
}
